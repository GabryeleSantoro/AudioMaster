# AudioMaster — GitHub Pages

This branch is the source for the product landing page.

**GitHub → Settings → Pages → Deploy from branch:** `website` / `/ (root)`.

Site files: `index.html`, `styles.css`, `assets/`.
